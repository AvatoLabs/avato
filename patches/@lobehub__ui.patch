diff --git a/es/Modal/Modal.mjs b/es/Modal/Modal.mjs
index 81ec2ee1e229209afd36d549caea2d838fbdd2e4..b472dba02f35fbb9b1b5b581fbc53a656f53c89a 100644
--- a/es/Modal/Modal.mjs
+++ b/es/Modal/Modal.mjs
@@ -10,7 +10,7 @@ import { cssVar, cx, useResponsive } from "antd-style";
 import { Maximize2, Minimize2, X } from "lucide-react";
 
 //#region src/Modal/Modal.tsx
-const Modal$1 = memo(({ panelRef, allowFullscreen, children, title = " ", className, classNames, width = 700, onCancel, open, destroyOnHidden, paddings, height = "75dvh", enableResponsive = true, footer, styles: customStyles, okText, onOk, cancelText, okButtonProps, cancelButtonProps, confirmLoading, ...rest }) => {
+const Modal$1 = memo(({ panelRef, allowFullscreen, children, title = " ", className, classNames, width = 700, onCancel, open, destroyOnHidden, paddings, height = "75dvh", enableResponsive = true, footer, styles: customStyles, okText, onOk, cancelText, okButtonProps, cancelButtonProps, confirmLoading, mask, ...rest }) => {
 	const [fullscreen, setFullscreen] = useState(false);
 	const { mobile } = useResponsive();
 	const hideFooter = footer === false || footer === null;
@@ -60,6 +60,7 @@ const Modal$1 = memo(({ panelRef, allowFullscreen, children, title = " ", classN
 			},
 			onClose: onCancel,
 			...rest,
+			mask: { closable: true, ...(mask && typeof mask === "object" && mask !== null ? mask : {}) },
 			children
 		})
 	});
@@ -67,7 +68,6 @@ const Modal$1 = memo(({ panelRef, allowFullscreen, children, title = " ", classN
 		theme: { token: { colorBgElevated: cssVar.colorBgContainer } },
 		children: /* @__PURE__ */ jsx(Modal, {
 			closable: true,
-			maskClosable: true,
 			cancelText,
 			className: cx(styles.content, className),
 			closeIcon: /* @__PURE__ */ jsx(Icon_default, {
@@ -105,6 +105,7 @@ const Modal$1 = memo(({ panelRef, allowFullscreen, children, title = " ", classN
 			onCancel,
 			onOk,
 			...rest,
+			mask: { closable: true, ...(mask && typeof mask === "object" && mask !== null ? mask : {}) },
 			children
 		})
 	});
diff --git a/es/Modal/ModalStackItem.mjs b/es/Modal/ModalStackItem.mjs
index 94086f7513ef29b0a1cf7b695b35ef2a22eaa1d7..f47081ec3c40ec7eb6b9d70d6af846c06e74e149 100644
--- a/es/Modal/ModalStackItem.mjs
+++ b/es/Modal/ModalStackItem.mjs
@@ -14,7 +14,7 @@ const ModalStackItem = memo(({ id, props, onClose, onUpdate, onDestroy }) => {
 	const stableAfterOpenChange = useEventCallback(afterOpenChange ?? noop);
 	const stableOnCancel = useEventCallback(onCancel ?? noop);
 	const close = useEventCallback(() => onClose(id));
-	const setCanDismissByClickOutside = useEventCallback((value) => onUpdate(id, { maskClosable: value }));
+	const setCanDismissByClickOutside = useEventCallback((value) => onUpdate(id, { mask: { closable: value } }));
 	const stableContextValue = useMemo(() => ({
 		close,
 		setCanDismissByClickOutside
diff --git a/es/Modal/RawModalStackItem.mjs b/es/Modal/RawModalStackItem.mjs
index 7f79ef2a2d3905ca4dadd296c82b03f30be9d38e..e3e45c0e44e890ff2292f979d060e13c7e7b4bdd 100644
--- a/es/Modal/RawModalStackItem.mjs
+++ b/es/Modal/RawModalStackItem.mjs
@@ -10,7 +10,7 @@ const RawModalStackItem = memo(({ component: Component, id, onClose, onUpdate, o
 	const stableOnClose = useEventCallback(onClose);
 	const close = useEventCallback(() => stableOnClose(id));
 	const setCanDismissByClickOutside = useEventCallback((value) => {
-		onUpdate(id, { maskClosable: value });
+		onUpdate(id, { mask: { closable: value } });
 	});
 	const contextValue = useMemo(() => ({
 		close,
diff --git a/es/Modal/imperative.mjs b/es/Modal/imperative.mjs
index 6401038f6c1f9c59c9d9098affba03ace863aec8..6fa140794628a1f7e9ed773b4680b564334d0d3b 100644
--- a/es/Modal/imperative.mjs
+++ b/es/Modal/imperative.mjs
@@ -34,12 +34,25 @@ const updateModal = (id, nextProps) => {
 		if (item.id !== id) return item;
 		if (item.kind !== "modal") return item;
 		changed = true;
+		const mergedProps = {
+			...item.props,
+			...nextProps
+		};
+		if (
+			nextProps.mask !== undefined &&
+			item.props.mask !== undefined &&
+			typeof item.props.mask === "object" &&
+			item.props.mask !== null &&
+			typeof nextProps.mask === "object" &&
+			nextProps.mask !== null &&
+			!Array.isArray(item.props.mask) &&
+			!Array.isArray(nextProps.mask)
+		) {
+			mergedProps.mask = { ...item.props.mask, ...nextProps.mask };
+		}
 		return {
 			...item,
-			props: {
-				...item.props,
-				...nextProps
-			}
+			props: mergedProps
 		};
 	});
 	if (changed) notify();
@@ -50,12 +63,25 @@ const updateRawProps = (id, nextProps) => {
 		if (item.id !== id) return item;
 		if (item.kind !== "raw") return item;
 		changed = true;
+		const mergedProps = {
+			...item.props,
+			...nextProps
+		};
+		if (
+			nextProps.mask !== undefined &&
+			item.props.mask !== undefined &&
+			typeof item.props.mask === "object" &&
+			item.props.mask !== null &&
+			typeof nextProps.mask === "object" &&
+			nextProps.mask !== null &&
+			!Array.isArray(item.props.mask) &&
+			!Array.isArray(nextProps.mask)
+		) {
+			mergedProps.mask = { ...item.props.mask, ...nextProps.mask };
+		}
 		return {
 			...item,
-			props: {
-				...item.props,
-				...nextProps
-			}
+			props: mergedProps
 		};
 	});
 	if (changed) notify();
@@ -160,7 +186,7 @@ const createModal = (props) => {
 	return {
 		close: () => closeModal(id),
 		destroy: () => destroyModal(id),
-		setCanDismissByClickOutside: (value) => updateModal(id, { maskClosable: value }),
+		setCanDismissByClickOutside: (value) => updateModal(id, { mask: { closable: value } }),
 		update: (nextProps) => updateModal(id, nextProps)
 	};
 };
@@ -178,7 +204,7 @@ function createRawModal(component, props, options) {
 	return {
 		close: () => closeModal(id),
 		destroy: () => destroyModal(id),
-		setCanDismissByClickOutside: (value) => updateRawProps(id, { maskClosable: value }),
+		setCanDismissByClickOutside: (value) => updateRawProps(id, { mask: { closable: value } }),
 		update: (nextProps) => updateRawProps(id, nextProps)
 	};
 }
diff --git a/es/color/colors/green.mjs b/es/color/colors/green.mjs
--- a/es/color/colors/green.mjs
+++ b/es/color/colors/green.mjs
@@ -2,63 +2,63 @@
 const green = {
 	dark: [
 		"#000503",
-		"#001d12",
-		"#002d1d",
-		"#003f28",
-		"#005232",
-		"#00653c",
-		"#007944",
-		"#1b8d4d",
-		"#3ba05a",
-		"#62c473",
-		"#96cd92",
-		"#cde6c3",
+		"#001d14",
+		"#002e1f",
+		"#00402b",
+		"#005237",
+		"#006543",
+		"#007a52",
+		"#0a8f62",
+		"#0b9b6a",
+		"#10a37f",
+		"#19c37d",
+		"#4dd4a5",
 		"#ffffff"
 	],
 	darkA: [
 		"rgba(0, 250, 150, 0.02)",
 		"rgba(0, 242, 150, 0.12)",
-		"rgba(0, 250, 161, 0.18)",
-		"rgba(0, 252, 160, 0.25)",
-		"rgba(0, 248, 152, 0.33)",
-		"rgba(0, 252, 150, 0.4)",
-		"rgba(0, 252, 142, 0.48)",
-		"rgba(48, 252, 137, 0.56)",
-		"rgba(94, 254, 143, 0.63)",
-		"rgba(120, 254, 145, 0.71)",
-		"rgba(185, 253, 180, 0.81)",
-		"rgba(225, 253, 214, 0.91)",
+		"rgba(0, 245, 168, 0.2)",
+		"rgba(0, 250, 170, 0.28)",
+		"rgba(0, 250, 168, 0.36)",
+		"rgba(0, 252, 160, 0.44)",
+		"rgba(0, 250, 180, 0.52)",
+		"rgba(16, 250, 150, 0.6)",
+		"rgba(25, 255, 170, 0.68)",
+		"rgba(16, 163, 127, 0.9)",
+		"rgba(25, 195, 125, 0.92)",
+		"rgba(120, 230, 200, 0.85)",
 		"#ffffff"
 	],
 	light: [
 		"#ffffff",
-		"#f4fdeb",
-		"#e7f8dd",
-		"#d8f2ce",
-		"#c7eabd",
-		"#b4e1ac",
-		"#a0d79b",
-		"#89cc8a",
-		"#71c179",
-		"#379d4a",
-		"#007944",
-		"#003f28",
-		"#000503"
+		"#ecfdf5",
+		"#d1fae5",
+		"#a7f3d0",
+		"#6ee7b7",
+		"#34d399",
+		"#10b981",
+		"#059669",
+		"#047857",
+		"#10a37f",
+		"#0d8a6a",
+		"#065f46",
+		"#022c22"
 	],
 	lightA: [
 		"rgba(255, 255, 255, 0.02)",
-		"rgba(117, 230, 5, 0.06)",
-		"rgba(84, 205, 12, 0.10)",
-		"rgba(60, 190, 10, 0.14)",
-		"rgba(40, 174, 1, 0.26)",
-		"rgba(28, 164, 3, 0.33)",
-		"rgba(18, 155, 5, 0.4)",
-		"rgba(4, 146, 6, 0.47)",
-		"rgba(1, 144, 16, 0.56)",
-		"rgba(1, 143, 28, 0.67)",
-		"#007944",
-		"#003f28",
-		"#000503"
+		"rgba(16, 185, 129, 0.08)",
+		"rgba(5, 180, 120, 0.1)",
+		"rgba(4, 190, 110, 0.14)",
+		"rgba(5, 185, 105, 0.18)",
+		"rgba(0, 180, 100, 0.22)",
+		"rgba(0, 175, 100, 0.28)",
+		"rgba(0, 155, 95, 0.35)",
+		"rgba(0, 140, 90, 0.42)",
+		"rgba(16, 163, 127, 0.55)",
+		"rgba(13, 138, 106, 0.62)",
+		"rgba(6, 95, 70, 0.75)",
+		"rgba(2, 44, 34, 0.9)"
 	]
 };
 var green_default = green;
diff --git a/es/color/colors/cyan.mjs b/es/color/colors/cyan.mjs
--- a/es/color/colors/cyan.mjs
+++ b/es/color/colors/cyan.mjs
diff --git a/es/Tooltip/TooltipStandalone.mjs b/es/Tooltip/TooltipStandalone.mjs
index a7821925..faf5937e 100644
--- a/es/Tooltip/TooltipStandalone.mjs
+++ b/es/Tooltip/TooltipStandalone.mjs
@@ -97,7 +97,7 @@ const TooltipStandalone = memo(({ children, title, arrow = false, className, cla
 				return cloneElement(children, {
 					...mergeProps(restProps, children.props, resolvedProps),
 					ref: mergeRefs([
-						children.ref,
+						children.props.ref,
 						props.ref,
 						refProp,
 						triggerCallbackRef
diff --git a/es/Tooltip/TooltipInGroup.mjs b/es/Tooltip/TooltipInGroup.mjs
index f45c9eb7..95a98b97 100644
--- a/es/Tooltip/TooltipInGroup.mjs
+++ b/es/Tooltip/TooltipInGroup.mjs
@@ -86,7 +86,7 @@ const TooltipInGroup = ({ children, ref: refProp, arrow, className, classNames,
 		return cloneElement(childElement, {
 			...mergeProps(restProps, childElement.props, resolvedProps),
 			ref: mergeRefs([
-				childElement.ref,
+				childElement.props.ref,
 				renderProps.ref,
 				refProp
 			])
diff --git a/es/base-ui/Popover/atoms.mjs b/es/base-ui/Popover/atoms.mjs
index 82615977..56b19cae 100644
--- a/es/base-ui/Popover/atoms.mjs
+++ b/es/base-ui/Popover/atoms.mjs
@@ -37,7 +37,7 @@ const PopoverTriggerElement = ({ children, className, nativeButton, ref: refProp
 				...mergedProps,
 				className: cx(baseClassName, extraClassName),
 				ref: mergeRefs([
-					children.ref,
+					children.props.ref,
 					props.ref,
 					refProp
 				])
diff --git a/es/base-ui/Popover/PopoverInGroup.mjs b/es/base-ui/Popover/PopoverInGroup.mjs
index 88624adc..83238490 100644
--- a/es/base-ui/Popover/PopoverInGroup.mjs
+++ b/es/base-ui/Popover/PopoverInGroup.mjs
@@ -47,7 +47,7 @@ const PopoverInGroup = ({ children, ref: refProp, ...props }) => {
 				...mergedProps,
 				className: cx(mergedProps.className, triggerClassName),
 				ref: mergeRefs([
-					children.ref,
+					children.props.ref,
 					renderProps.ref,
 					refProp
 				])
diff --git a/es/base-ui/DropdownMenu/atoms.mjs b/es/base-ui/DropdownMenu/atoms.mjs
index 90eed704..f4cb90d8 100644
--- a/es/base-ui/DropdownMenu/atoms.mjs
+++ b/es/base-ui/DropdownMenu/atoms.mjs
@@ -42,7 +42,7 @@ const DropdownMenuTrigger = ({ children, className, nativeButton, ref: refProp,
 			...mergedProps,
 			className: clsx(CLASSNAMES.DropdownMenuTrigger, className, mergedProps.className),
 			ref: mergeRefs([
-				children.ref,
+				children.props.ref,
 				props.ref,
 				refProp
 			])
diff --git a/es/base-ui/Modal/atoms.mjs b/es/base-ui/Modal/atoms.mjs
index d376b100..64d2db76 100644
--- a/es/base-ui/Modal/atoms.mjs
+++ b/es/base-ui/Modal/atoms.mjs
@@ -164,7 +164,7 @@ const ModalTrigger = ({ children, className, nativeButton, ref: refProp, ...rest
 		return cloneElement(children, {
 			...mergeProps(children.props, resolvedProps),
 			ref: mergeRefs([
-				children.ref,
+				children.props.ref,
 				props.ref,
 				refProp
 			])
diff --git a/es/base-ui/Select/atoms.mjs b/es/base-ui/Select/atoms.mjs
index c64c2ff2..6ae2ec50 100644
--- a/es/base-ui/Select/atoms.mjs
+++ b/es/base-ui/Select/atoms.mjs
@@ -46,7 +46,7 @@ const SelectTrigger = ({ children, className, nativeButton, shadow, size = "midd
 				...mergedProps,
 				className: cx(baseClassName, childClassName, extraClassName),
 				ref: mergeRefs([
-					children.ref,
+					children.props.ref,
 					props.ref,
 					refProp
 				])
@@ -7,12 +7,12 @@
 		"#005245",
 		"#006c5b",
 		"#008772",
-		"#2fa28a",
-		"#55bca4",
-		"#75d7be",
-		"#95f3d9",
-		"#bdf7e4",
-		"#dffcf0",
+		"#0a9fb0",
+		"#0fb4c9",
+		"#14c8e0",
+		"#22d3ee",
+		"#5eead4",
+		"#99f6e4",
 		"#ffffff"
 	],
 	darkA: [
@@ -22,43 +22,43 @@
 		"rgba(0, 248, 209, 0.33)",
 		"rgba(0, 251, 212, 0.43)",
 		"rgba(0, 255, 215, 0.53)",
-		"rgba(73, 253, 216, 0.64)",
-		"rgba(115, 254, 222, 0.74)",
-		"rgba(138, 253, 224, 0.85)",
-		"rgba(155, 253, 226, 0.96)",
-		"rgba(195, 255, 235, 0.97)",
-		"rgba(225, 255, 242, 0.99)",
+		"rgba(20, 250, 230, 0.64)",
+		"rgba(40, 250, 235, 0.72)",
+		"rgba(60, 250, 240, 0.8)",
+		"rgba(34, 211, 238, 0.9)",
+		"rgba(94, 234, 212, 0.92)",
+		"rgba(153, 246, 228, 0.94)",
 		"#ffffff"
 	],
 	light: [
 		"#ffffff",
-		"#f9fffb",
-		"#effff8",
-		"#e3fff4",
-		"#d8fef0",
-		"#ccfcec",
-		"#c0fae8",
-		"#b3f8e3",
-		"#a5f6de",
-		"#95f3d9",
-		"#2fa28a",
-		"#005245",
+		"#f0fdfa",
+		"#ccfbf1",
+		"#99f6e4",
+		"#5eead4",
+		"#2dd4bf",
+		"#14b8a6",
+		"#0d9488",
+		"#0f766e",
+		"#14b8a6",
+		"#0d9488",
+		"#115e59",
 		"#000503"
 	],
 	lightA: [
 		"rgba(255, 255, 255, 0.01)",
-		"rgba(55, 255, 122, 0.03)",
-		"rgba(26, 255, 155, 0.07)",
-		"rgba(0, 255, 155, 0.11)",
-		"rgba(11, 249, 161, 0.16)",
-		"rgba(0, 240, 160, 0.2)",
-		"rgba(3, 235, 163, 0.25)",
-		"rgba(2, 232, 162, 0.3)",
-		"rgba(5, 230, 163, 0.36)",
-		"rgba(3, 226, 165, 0.42)",
-		"rgba(1, 142, 112, 0.82)",
-		"#005245",
-		"#000503"
+		"rgba(45, 212, 191, 0.06)",
+		"rgba(20, 184, 166, 0.1)",
+		"rgba(15, 180, 160, 0.14)",
+		"rgba(13, 175, 155, 0.18)",
+		"rgba(10, 170, 150, 0.22)",
+		"rgba(8, 165, 145, 0.28)",
+		"rgba(5, 155, 140, 0.34)",
+		"rgba(5, 140, 130, 0.4)",
+		"rgba(20, 184, 166, 0.5)",
+		"rgba(13, 148, 136, 0.58)",
+		"rgba(17, 94, 89, 0.72)",
+		"rgba(0, 5, 3, 0.9)"
 	]
 };
var cyan_default = cyan;
diff --git a/es/Flex/FlexBasic.mjs b/es/Flex/FlexBasic.mjs
--- a/es/Flex/FlexBasic.mjs
+++ b/es/Flex/FlexBasic.mjs
@@ -5,12 +5,25 @@ import { jsx } from "react/jsx-runtime";
 
 //#region src/Flex/FlexBasic.tsx
-const FlexBasic = ({ visible, flex, gap, direction, horizontal, align, justify, distribution, height, width, allowShrink, padding, paddingInline, paddingBlock, prefixCls, as: Container = "div", className, style, children, wrap, ref, ...props }) => {
+const FlexBasic = ({ visible, flex, gap, direction, horizontal, vertical, inverse, align, justify, distribution, height, width, allowShrink, padding, paddingInline, paddingBlock, prefixCls, as: Container = "div", className, style, children, wrap, ref, ...props }) => {
 	const justifyContent = justify || distribution;
+	const resolvedDirection = (() => {
+		const legacyDirection = direction ?? (vertical ? "vertical" : undefined);
+		if (!inverse) return legacyDirection;
+		if (legacyDirection === "horizontal") return "horizontal-reverse";
+		if (legacyDirection === "horizontal-reverse") return "horizontal";
+		if (legacyDirection === "vertical-reverse") return "vertical";
+		if (legacyDirection === "vertical") return "vertical-reverse";
+		if (horizontal) return "horizontal-reverse";
+		return "vertical-reverse";
+	})();
 	const calcWidth = () => {
-		if (isHorizontal(direction, horizontal) && !width && isSpaceDistribution(justifyContent)) return "100%";
+		if (isHorizontal(resolvedDirection, horizontal) && !width && isSpaceDistribution(justifyContent)) return "100%";
 		return getCssValue(width);
 	};
 	const finalWidth = calcWidth();
 	const mergedStyle = {
 		...flex !== void 0 ? { "--lobe-flex": String(flex) } : {},
-		...direction || horizontal ? { "--lobe-flex-direction": getFlexDirection(direction, horizontal) } : {},
+		...resolvedDirection || horizontal ? { "--lobe-flex-direction": getFlexDirection(resolvedDirection, horizontal) } : {},
 		...wrap !== void 0 ? { "--lobe-flex-wrap": wrap } : {},
 		...justifyContent !== void 0 ? { "--lobe-flex-justify": justifyContent } : {},
 		...align !== void 0 ? { "--lobe-flex-align": align } : {},
diff --git a/es/Modal/Modal.mjs b/es/Modal/Modal.mjs
--- a/es/Modal/Modal.mjs
+++ b/es/Modal/Modal.mjs
@@ -10,8 +10,12 @@ import { cssVar, cx, useResponsive } from "antd-style";
 import { Maximize2, Minimize2, X } from "lucide-react";
 
 //#region src/Modal/Modal.tsx
-const Modal$1 = memo(({ panelRef, allowFullscreen, children, title = " ", className, classNames, width = 700, onCancel, open, destroyOnHidden, paddings, height = "75dvh", enableResponsive = true, footer, styles: customStyles, okText, onOk, cancelText, okButtonProps, cancelButtonProps, confirmLoading, mask, ...rest }) => {
+const Modal$1 = memo(({ panelRef, allowFullscreen, children, title = " ", className, classNames, width = 700, onCancel, open, destroyOnHidden, paddings, height = "75dvh", enableResponsive = true, footer, styles: customStyles, okText, onOk, cancelText, okButtonProps, cancelButtonProps, confirmLoading, focusable, focusTriggerAfterClose, mask, ...rest }) => {
 	const [fullscreen, setFullscreen] = useState(false);
 	const { mobile } = useResponsive();
+	const mergedFocusable = focusTriggerAfterClose === void 0 ? focusable : {
+		...(focusable && typeof focusable === "object" ? focusable : {}),
+		focusTriggerAfterClose
+	};
 	const hideFooter = footer === false || footer === null;
 	if (enableResponsive && mobile) return /* @__PURE__ */ jsx(ConfigProvider, {
 		theme: { token: { colorBgElevated: cssVar.colorBgContainer } },
@@ -59,6 +63,7 @@ const Modal$1 = memo(({ panelRef, allowFullscreen, children, title = " ", classN
 			},
 			onClose: onCancel,
 			...rest,
+			focusable: mergedFocusable,
 			mask: { closable: true, ...(mask && typeof mask === "object" && mask !== null ? mask : {}) },
 			children
 		})
@@ -103,6 +108,7 @@ const Modal$1 = memo(({ panelRef, allowFullscreen, children, title = " ", classN
 			onCancel,
 			onOk,
 			...rest,
+			focusable: mergedFocusable,
 			mask: { closable: true, ...(mask && typeof mask === "object" && mask !== null ? mask : {}) },
 			children
 		})
