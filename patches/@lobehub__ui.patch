diff --git a/es/Modal/Modal.mjs b/es/Modal/Modal.mjs
index 81ec2ee..a6a7f75 100644
--- a/es/Modal/Modal.mjs
+++ b/es/Modal/Modal.mjs
@@ -10,8 +10,12 @@ import { cssVar, cx, useResponsive } from "antd-style";
 import { Maximize2, Minimize2, X } from "lucide-react";
 
 //#region src/Modal/Modal.tsx
-const Modal$1 = memo(({ panelRef, allowFullscreen, children, title = " ", className, classNames, width = 700, onCancel, open, destroyOnHidden, paddings, height = "75dvh", enableResponsive = true, footer, styles: customStyles, okText, onOk, cancelText, okButtonProps, cancelButtonProps, confirmLoading, ...rest }) => {
+const Modal$1 = memo(({ panelRef, allowFullscreen, children, title = " ", className, classNames, width = 700, onCancel, open, destroyOnHidden, paddings, height = "75dvh", enableResponsive = true, footer, styles: customStyles, okText, onOk, cancelText, okButtonProps, cancelButtonProps, confirmLoading, focusable, focusTriggerAfterClose, mask, ...rest }) => {
 	const [fullscreen, setFullscreen] = useState(false);
+	const mergedFocusable = focusTriggerAfterClose === void 0 ? focusable : {
+		...(focusable && typeof focusable === "object" ? focusable : {}),
+		focusTriggerAfterClose
+	};
 	const { mobile } = useResponsive();
 	const hideFooter = footer === false || footer === null;
 	if (enableResponsive && mobile) return /* @__PURE__ */ jsx(ConfigProvider, {
@@ -58,6 +62,8 @@ const Modal$1 = memo(({ panelRef, allowFullscreen, children, title = " ", classN
 					...customStyles?.body
 				}
 			},
+			focusable: mergedFocusable,
+			mask: { closable: true, ...(mask && typeof mask === "object" && mask !== null ? mask : {}) },
 			onClose: onCancel,
 			...rest,
 			children
@@ -67,7 +73,6 @@ const Modal$1 = memo(({ panelRef, allowFullscreen, children, title = " ", classN
 		theme: { token: { colorBgElevated: cssVar.colorBgContainer } },
 		children: /* @__PURE__ */ jsx(Modal, {
 			closable: true,
-			maskClosable: true,
 			cancelText,
 			className: cx(styles.content, className),
 			closeIcon: /* @__PURE__ */ jsx(Icon_default, {
@@ -103,6 +108,8 @@ const Modal$1 = memo(({ panelRef, allowFullscreen, children, title = " ", classN
 				}
 			},
 			onCancel,
+			focusable: mergedFocusable,
+			mask: { closable: true, ...(mask && typeof mask === "object" && mask !== null ? mask : {}) },
 			onOk,
 			...rest,
 			children
@@ -114,4 +121,4 @@ var Modal_default = Modal$1;
 
 //#endregion
 export { Modal_default as default };
-//# sourceMappingURL=Modal.mjs.map
\ No newline at end of file
+//# sourceMappingURL=Modal.mjs.map
diff --git a/es/Modal/ModalStackItem.mjs b/es/Modal/ModalStackItem.mjs
index 94086f7..de88404 100644
--- a/es/Modal/ModalStackItem.mjs
+++ b/es/Modal/ModalStackItem.mjs
@@ -14,7 +14,7 @@ const ModalStackItem = memo(({ id, props, onClose, onUpdate, onDestroy }) => {
 	const stableAfterOpenChange = useEventCallback(afterOpenChange ?? noop);
 	const stableOnCancel = useEventCallback(onCancel ?? noop);
 	const close = useEventCallback(() => onClose(id));
-	const setCanDismissByClickOutside = useEventCallback((value) => onUpdate(id, { maskClosable: value }));
+	const setCanDismissByClickOutside = useEventCallback((value) => onUpdate(id, { mask: { closable: value } }));
 	const stableContextValue = useMemo(() => ({
 		close,
 		setCanDismissByClickOutside
@@ -52,4 +52,4 @@ ModalStackItem.displayName = "ModalStackItem";
 
 //#endregion
 export { ModalStackItem };
-//# sourceMappingURL=ModalStackItem.mjs.map
\ No newline at end of file
+//# sourceMappingURL=ModalStackItem.mjs.map
diff --git a/es/Modal/RawModalStackItem.mjs b/es/Modal/RawModalStackItem.mjs
index 7f79ef2..8ec2557 100644
--- a/es/Modal/RawModalStackItem.mjs
+++ b/es/Modal/RawModalStackItem.mjs
@@ -10,7 +10,7 @@ const RawModalStackItem = memo(({ component: Component, id, onClose, onUpdate, o
 	const stableOnClose = useEventCallback(onClose);
 	const close = useEventCallback(() => stableOnClose(id));
 	const setCanDismissByClickOutside = useEventCallback((value) => {
-		onUpdate(id, { maskClosable: value });
+		onUpdate(id, { mask: { closable: value } });
 	});
 	const contextValue = useMemo(() => ({
 		close,
@@ -31,4 +31,4 @@ RawModalStackItem.displayName = "RawModalStackItem";
 
 //#endregion
 export { RawModalStackItem };
-//# sourceMappingURL=RawModalStackItem.mjs.map
\ No newline at end of file
+//# sourceMappingURL=RawModalStackItem.mjs.map
diff --git a/es/Modal/imperative.mjs b/es/Modal/imperative.mjs
index 6401038..8230fbb 100644
--- a/es/Modal/imperative.mjs
+++ b/es/Modal/imperative.mjs
@@ -33,13 +33,26 @@ const updateModal = (id, nextProps) => {
 	modalStack = modalStack.map((item) => {
 		if (item.id !== id) return item;
 		if (item.kind !== "modal") return item;
+		const mergedProps = {
+			...item.props,
+			...nextProps
+		};
+		if (
+			nextProps.mask !== void 0 &&
+			item.props.mask !== void 0 &&
+			typeof item.props.mask === "object" &&
+			item.props.mask !== null &&
+			typeof nextProps.mask === "object" &&
+			nextProps.mask !== null &&
+			!Array.isArray(item.props.mask) &&
+			!Array.isArray(nextProps.mask)
+		) {
+			mergedProps.mask = { ...item.props.mask, ...nextProps.mask };
+		}
 		changed = true;
 		return {
 			...item,
-			props: {
-				...item.props,
-				...nextProps
-			}
+			props: mergedProps
 		};
 	});
 	if (changed) notify();
@@ -49,13 +62,26 @@ const updateRawProps = (id, nextProps) => {
 	modalStack = modalStack.map((item) => {
 		if (item.id !== id) return item;
 		if (item.kind !== "raw") return item;
+		const mergedProps = {
+			...item.props,
+			...nextProps
+		};
+		if (
+			nextProps.mask !== void 0 &&
+			item.props.mask !== void 0 &&
+			typeof item.props.mask === "object" &&
+			item.props.mask !== null &&
+			typeof nextProps.mask === "object" &&
+			nextProps.mask !== null &&
+			!Array.isArray(item.props.mask) &&
+			!Array.isArray(nextProps.mask)
+		) {
+			mergedProps.mask = { ...item.props.mask, ...nextProps.mask };
+		}
 		changed = true;
 		return {
 			...item,
-			props: {
-				...item.props,
-				...nextProps
-			}
+			props: mergedProps
 		};
 	});
 	if (changed) notify();
@@ -160,7 +186,7 @@ const createModal = (props) => {
 	return {
 		close: () => closeModal(id),
 		destroy: () => destroyModal(id),
-		setCanDismissByClickOutside: (value) => updateModal(id, { maskClosable: value }),
+		setCanDismissByClickOutside: (value) => updateModal(id, { mask: { closable: value } }),
 		update: (nextProps) => updateModal(id, nextProps)
 	};
 };
@@ -178,11 +204,11 @@ function createRawModal(component, props, options) {
 	return {
 		close: () => closeModal(id),
 		destroy: () => destroyModal(id),
-		setCanDismissByClickOutside: (value) => updateRawProps(id, { maskClosable: value }),
+		setCanDismissByClickOutside: (value) => updateRawProps(id, { mask: { closable: value } }),
 		update: (nextProps) => updateRawProps(id, nextProps)
 	};
 }
 
 //#endregion
 export { ModalHost, createModal, createRawModal };
-//# sourceMappingURL=imperative.mjs.map
\ No newline at end of file
+//# sourceMappingURL=imperative.mjs.map
diff --git a/es/Tooltip/TooltipInGroup.mjs b/es/Tooltip/TooltipInGroup.mjs
index ac11a7a..bbd5f03 100644
--- a/es/Tooltip/TooltipInGroup.mjs
+++ b/es/Tooltip/TooltipInGroup.mjs
@@ -78,19 +78,19 @@ const TooltipInGroup = ({ children, ref: refProp, arrow, className, classNames,
 	const { isNativeButtonTriggerElement } = useNativeButton({ children });
 	const childElement = isValidElement(children) ? children : null;
 	const renderTrigger = useCallback((renderProps) => {
-		const resolvedProps = (() => {
-			if (isNativeButtonTriggerElement) return renderProps;
-			const { type, ref: triggerRef, ...triggerRest } = renderProps;
-			return triggerRest;
-		})();
-		return cloneElement(childElement, {
-			...mergeProps(restProps, childElement.props, resolvedProps),
-			ref: mergeRefs([
-				childElement.ref,
-				renderProps.ref,
-				refProp
-			])
-		});
+			const resolvedProps = (() => {
+				if (isNativeButtonTriggerElement) return renderProps;
+				const { type, ref: triggerRef, ...triggerRest } = renderProps;
+				return triggerRest;
+			})();
+				return cloneElement(childElement, {
+					...mergeProps(restProps, childElement.props, resolvedProps),
+					ref: mergeRefs([
+						childElement.props?.ref ?? null,
+						renderProps.ref,
+						refProp
+					])
+			});
 	}, [
 		childElement,
 		isNativeButtonTriggerElement,
@@ -120,4 +120,4 @@ TooltipInGroup.displayName = "TooltipInGroup";
 
 //#endregion
 export { TooltipInGroup };
-//# sourceMappingURL=TooltipInGroup.mjs.map
\ No newline at end of file
+//# sourceMappingURL=TooltipInGroup.mjs.map
diff --git a/es/Tooltip/TooltipStandalone.mjs b/es/Tooltip/TooltipStandalone.mjs
index c31115a..73a55b9 100644
--- a/es/Tooltip/TooltipStandalone.mjs
+++ b/es/Tooltip/TooltipStandalone.mjs
@@ -93,14 +93,14 @@ const TooltipStandalone = memo(({ children, title, arrow = false, className, cla
 					if (isNativeButtonTriggerElement) return props;
 					const { type, ref: triggerRef, ...triggerRest } = props;
 					return triggerRest;
-				})();
-				return cloneElement(children, {
-					...mergeProps(restProps, children.props, resolvedProps),
-					ref: mergeRefs([
-						children.ref,
-						props.ref,
-						refProp,
-						triggerCallbackRef
+					})();
+					return cloneElement(children, {
+						...mergeProps(restProps, children.props, resolvedProps),
+						ref: mergeRefs([
+							children.props?.ref ?? null,
+							props.ref,
+							refProp,
+							triggerCallbackRef
 					])
 				});
 			}
@@ -189,4 +189,4 @@ TooltipStandalone.displayName = "TooltipStandalone";
 
 //#endregion
 export { TooltipStandalone };
-//# sourceMappingURL=TooltipStandalone.mjs.map
\ No newline at end of file
+//# sourceMappingURL=TooltipStandalone.mjs.map
diff --git a/es/base-ui/DropdownMenu/atoms.mjs b/es/base-ui/DropdownMenu/atoms.mjs
index 6ff8513..996fec0 100644
--- a/es/base-ui/DropdownMenu/atoms.mjs
+++ b/es/base-ui/DropdownMenu/atoms.mjs
@@ -37,15 +37,15 @@ const DropdownMenuTrigger = ({ children, className, nativeButton, ref: refProp,
 			const { type, ...restProps } = props;
 			return restProps;
 		})();
-		const mergedProps = mergeProps(children.props, resolvedProps);
-		return cloneElement(children, {
-			...mergedProps,
-			className: clsx(CLASSNAMES.DropdownMenuTrigger, className, mergedProps.className),
-			ref: mergeRefs([
-				children.ref,
-				props.ref,
-				refProp
-			])
+			const mergedProps = mergeProps(children.props, resolvedProps);
+			return cloneElement(children, {
+				...mergedProps,
+				className: clsx(CLASSNAMES.DropdownMenuTrigger, className, mergedProps.className),
+				ref: mergeRefs([
+					children.props?.ref ?? null,
+					props.ref,
+					refProp
+				])
 		});
 	};
 	if (isValidElement(children)) return /* @__PURE__ */ jsx(Menu$1.Trigger, {
@@ -212,4 +212,4 @@ DropdownMenuSwitchItem.displayName = "DropdownMenuSwitchItem";
 
 //#endregion
 export { DropdownMenuCheckboxItemIndicator, DropdownMenuCheckboxItemPrimitive, DropdownMenuGroup, DropdownMenuGroupLabel, DropdownMenuItem, DropdownMenuItemContent, DropdownMenuItemDesc, DropdownMenuItemExtra, DropdownMenuItemIcon, DropdownMenuItemLabel, DropdownMenuItemLabelGroup, DropdownMenuPopup, DropdownMenuPortal, DropdownMenuPositioner, DropdownMenuRoot, DropdownMenuSeparator, DropdownMenuSubmenuArrow, DropdownMenuSubmenuRoot, DropdownMenuSubmenuTrigger, DropdownMenuSwitchItem, DropdownMenuTrigger };
-//# sourceMappingURL=atoms.mjs.map
\ No newline at end of file
+//# sourceMappingURL=atoms.mjs.map
diff --git a/es/base-ui/Modal/atoms.mjs b/es/base-ui/Modal/atoms.mjs
index d825edc..97d402c 100644
--- a/es/base-ui/Modal/atoms.mjs
+++ b/es/base-ui/Modal/atoms.mjs
@@ -160,14 +160,14 @@ const ModalTrigger = ({ children, className, nativeButton, ref: refProp, ...rest
 			if (isNativeButtonTriggerElement) return props;
 			const { type, ...restProps } = props;
 			return restProps;
-		})();
-		return cloneElement(children, {
-			...mergeProps(children.props, resolvedProps),
-			ref: mergeRefs([
-				children.ref,
-				props.ref,
-				refProp
-			])
+			})();
+			return cloneElement(children, {
+				...mergeProps(children.props, resolvedProps),
+				ref: mergeRefs([
+					children.props?.ref ?? null,
+					props.ref,
+					refProp
+				])
 		});
 	};
 	if (isValidElement(children)) return /* @__PURE__ */ jsx(Dialog.Trigger, {
@@ -187,4 +187,4 @@ const ModalTrigger = ({ children, className, nativeButton, ref: refProp, ...rest
 
 //#endregion
 export { ModalBackdrop, ModalClose, ModalContent, ModalDescription, ModalFooter, ModalHeader, ModalPopup, ModalPortal, ModalRoot, ModalTitle, ModalTrigger, ModalViewport, useModalActions, useModalOpen };
-//# sourceMappingURL=atoms.mjs.map
\ No newline at end of file
+//# sourceMappingURL=atoms.mjs.map
diff --git a/es/base-ui/Popover/PopoverInGroup.mjs b/es/base-ui/Popover/PopoverInGroup.mjs
index e7754cb..f98971c 100644
--- a/es/base-ui/Popover/PopoverInGroup.mjs
+++ b/es/base-ui/Popover/PopoverInGroup.mjs
@@ -42,15 +42,15 @@ const PopoverInGroup = ({ children, ref: refProp, ...props }) => {
 				const { type, ref: triggerRef, ...restProps } = renderProps;
 				return restProps;
 			})();
-			const mergedProps = mergeProps(children.props, resolvedProps);
-			return cloneElement(children, {
-				...mergedProps,
-				className: cx(mergedProps.className, triggerClassName),
-				ref: mergeRefs([
-					children.ref,
-					renderProps.ref,
-					refProp
-				])
+				const mergedProps = mergeProps(children.props, resolvedProps);
+				return cloneElement(children, {
+					...mergedProps,
+					className: cx(mergedProps.className, triggerClassName),
+					ref: mergeRefs([
+						children.props?.ref ?? null,
+						renderProps.ref,
+						refProp
+					])
 			});
 		}
 	});
@@ -67,4 +67,4 @@ PopoverInGroup.displayName = "PopoverInGroup";
 
 //#endregion
 export { PopoverInGroup };
-//# sourceMappingURL=PopoverInGroup.mjs.map
\ No newline at end of file
+//# sourceMappingURL=PopoverInGroup.mjs.map
diff --git a/es/base-ui/Popover/atoms.mjs b/es/base-ui/Popover/atoms.mjs
index 763e5f0..fa5a9c4 100644
--- a/es/base-ui/Popover/atoms.mjs
+++ b/es/base-ui/Popover/atoms.mjs
@@ -32,15 +32,15 @@ const PopoverTriggerElement = ({ children, className, nativeButton, ref: refProp
 			})();
 			const mergedProps = mergeProps(children.props, resolvedProps);
 			const baseClassName = typeof mergedProps.className === "function" ? mergedProps.className(state) : mergedProps.className;
-			const extraClassName = typeof className === "function" ? className(state) : className;
-			return cloneElement(children, {
-				...mergedProps,
-				className: cx(baseClassName, extraClassName),
-				ref: mergeRefs([
-					children.ref,
-					props.ref,
-					refProp
-				])
+				const extraClassName = typeof className === "function" ? className(state) : className;
+				return cloneElement(children, {
+					...mergedProps,
+					className: cx(baseClassName, extraClassName),
+					ref: mergeRefs([
+						children.props?.ref ?? null,
+						props.ref,
+						refProp
+					])
 			});
 		}
 	});
@@ -108,4 +108,4 @@ PopoverViewport.displayName = "PopoverViewport";
 
 //#endregion
 export { PopoverArrow, PopoverBackdrop, PopoverPopup, PopoverPortal, PopoverPositioner, PopoverRoot, PopoverTriggerElement, PopoverViewport };
-//# sourceMappingURL=atoms.mjs.map
\ No newline at end of file
+//# sourceMappingURL=atoms.mjs.map
diff --git a/es/base-ui/Select/atoms.mjs b/es/base-ui/Select/atoms.mjs
index 3ddca23..c71694b 100644
--- a/es/base-ui/Select/atoms.mjs
+++ b/es/base-ui/Select/atoms.mjs
@@ -41,15 +41,15 @@ const SelectTrigger = ({ children, className, nativeButton, shadow, size = "midd
 			})();
 			const mergedProps = mergeProps(children.props, resolvedProps);
 			const childClassName = typeof mergedProps.className === "function" ? mergedProps.className(state) : mergedProps.className;
-			const extraClassName = typeof className === "function" ? className(state) : className;
-			return cloneElement(children, {
-				...mergedProps,
-				className: cx(baseClassName, childClassName, extraClassName),
-				ref: mergeRefs([
-					children.ref,
-					props.ref,
-					refProp
-				])
+				const extraClassName = typeof className === "function" ? className(state) : className;
+				return cloneElement(children, {
+					...mergedProps,
+					className: cx(baseClassName, childClassName, extraClassName),
+					ref: mergeRefs([
+						children.props?.ref ?? null,
+						props.ref,
+						refProp
+					])
 			});
 		}
 	});
@@ -168,4 +168,4 @@ SelectArrow.displayName = "SelectArrow";
 
 //#endregion
 export { SelectArrow, SelectBackdrop, SelectGroup, SelectGroupLabel, SelectIcon, SelectItem, SelectItemIndicator, SelectItemText, SelectList, SelectPopup, SelectPortal, SelectPositioner, SelectRoot, SelectScrollDownArrow, SelectScrollUpArrow, SelectSeparator, SelectTrigger, SelectValue };
-//# sourceMappingURL=atoms.mjs.map
\ No newline at end of file
+//# sourceMappingURL=atoms.mjs.map
